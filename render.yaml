services:
  - type: web
    name: agent_store_backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      -  key:
         ENV: production
         GEMINI_API_KEY: AIzaSyCYMYyesyfZ2MQnMkeQILuG-OSrufVjK-k
